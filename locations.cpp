#include <QFile>
#include <QTextStream>
#include <QString>
#include <QStringList>
/*
QFile text;
text.setFileName(":/loc/locations/loc.txt");


while (!file.atEnd()) {
    QByteArray line = file.readLine();
    process_line(line);
}

QFile boardFile("Board.txt");
boardFile.open(QFile::ReadOnly);
if(!boardFile.isOpen())
{
    qDebug() << "File Was not opened!";
    std::exit(0);
}

QTextStream stream(&boardFile);
QString tempString;
for (int i = 0; i < 13.x; i++) {
    for (int j = 0; j < 13.y; j++) {
        stream >> tempString;
        m_boardData[i][j] = tempString.toInt();
    }
}
boardFile.close();
*/

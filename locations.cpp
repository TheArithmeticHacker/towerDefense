#include <QFile>
#include <QTextStream>
#include <QString>
#include <QStringList>
/*
QFile text;
text.setFileName(":/loc/locations/loc.txt");


while (!file.atEnd()) {
    QByteArray line = file.readLine();
    process_line(line);
}
*/
